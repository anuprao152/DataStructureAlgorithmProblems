@model IList<string>
    
@if (Model.Count == 0)
{
        for (int i = 0; i < 5; i++)
        {
            <div><label>Point @(i+1)</label>@Html.TextBox("names")</div>
        }
}   
else
{
    <ul>
        @foreach (var str in Model)
        {
           <li>@str</li>
        }
    </ul>
} 
